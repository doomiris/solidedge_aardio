import console; 
console.open();
import fsys;

var path = "S:\Cabinet\AMADP\"

var dlist = fsys.list(path,,"*.DFT");

for (i=1; #dlist){
	if !string.startWith(dlist[i], "008") continue;
	fsys.rename(path + dlist[i], path + "H" + dlist[i]);
	console.log( "H" + dlist[i])	
}

console.pause(true);