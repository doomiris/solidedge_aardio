import console; 
console.open();
import fsys;

/*
var path = "L:\Monaxis"

var dlist = fsys.list(path,,"*.DFT");

for (i=1; #dlist){
	if !string.startWith(dlist[i], "008") continue;
	var oldName = io.joinpath(path, dlist[i]);
	var newName = io.joinpath(path, "H" + dlist[i]);
	if io.exist(newName)
		io.remove(oldName);
	else 
		io.rename(oldName, newName);
	console.log( newName )	
}
*/


import fsys;
var path = "C:\Users\ccl100100\Desktop\New folder (3)\MA";
var list= fsys.list(path,,"*.docx");

for (i=1; #list){
	var oName = list[i];
	var uName = string.replace(oName, " D ", " M ");
	uName = string.replace(uName, " SS", " SS A");
	io.rename(io.joinpath(path,oName), io.joinpath(path,uName))
}


console.pause(true);