// seconn Connect to running SolidEdge
namespace seconn{
	ApplicationName="SolidEdge.Application";
	conn=function(appName){
		import com;
		return com.GetObject(appName); 
/*
		try{
			var comApp=com.GetObject(appName)
			return comApp; 
		}catch(e){
			import console
			console.log(e.id,e.description)
			console.pause(true)
		}
*/
	}
	seApp := self.conn(self.ApplicationName);
}
